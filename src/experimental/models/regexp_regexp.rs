/*
 * CrowdStrike API Specification
 *
 * Use this API specification as a reference for the API endpoints you can use to interact with your Falcon environment. These endpoints support authentication via OAuth2 and interact with detections and network containment. For detailed usage guides and examples, see our [documentation inside the Falcon console](https://falcon.crowdstrike.com/support/documentation).     To use the APIs described below, combine the base URL with the path shown for each API endpoint. For commercial cloud customers, your base URL is `https://api.crowdstrike.com`.    Each API endpoint requires authorization via an OAuth2 token. Your first API request should retrieve an OAuth2 token using the `oauth2/token` endpoint, such as `https://api.crowdstrike.com/oauth2/token`. For subsequent requests, include the OAuth2 token in an HTTP authorization header. Tokens expire after 30 minutes, after which you should make a new token request to continue making API requests.
 *
 * The version of the OpenAPI document: 2026-01-28T00:00:04Z
 *
 * Generated by: https://openapi-generator.tech
 */

use serde::{Deserialize, Serialize};

use crate::experimental::models;

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RegexpRegexp {
    #[serde(rename = "cond")]
    pub cond: i32,
    #[serde(rename = "expr")]
    pub expr: String,
    #[serde(rename = "longest")]
    pub longest: bool,
    #[serde(rename = "matchcap")]
    pub matchcap: i32,
    #[serde(rename = "maxBitStateLen")]
    pub max_bit_state_len: i32,
    #[serde(rename = "minInputLen")]
    pub min_input_len: i32,
    #[serde(rename = "mpool")]
    pub mpool: i32,
    #[serde(rename = "numSubexp")]
    pub num_subexp: i32,
    #[serde(rename = "onepass")]
    pub onepass: models::RegexpOnePassProg,
    #[serde(rename = "prefix")]
    pub prefix: String,
    #[serde(rename = "prefixBytes")]
    pub prefix_bytes: String,
    #[serde(rename = "prefixComplete")]
    pub prefix_complete: bool,
    #[serde(rename = "prefixEnd")]
    pub prefix_end: i32,
    #[serde(rename = "prefixRune")]
    pub prefix_rune: i32,
    #[serde(rename = "prog")]
    pub prog: models::SyntaxProg,
    #[serde(rename = "subexpNames")]
    pub subexp_names: Vec<String>,
}

impl RegexpRegexp {
    pub fn new(
        cond: i32,
        expr: String,
        longest: bool,
        matchcap: i32,
        max_bit_state_len: i32,
        min_input_len: i32,
        mpool: i32,
        num_subexp: i32,
        onepass: models::RegexpOnePassProg,
        prefix: String,
        prefix_bytes: String,
        prefix_complete: bool,
        prefix_end: i32,
        prefix_rune: i32,
        prog: models::SyntaxProg,
        subexp_names: Vec<String>,
    ) -> RegexpRegexp {
        RegexpRegexp {
            cond,
            expr,
            longest,
            matchcap,
            max_bit_state_len,
            min_input_len,
            mpool,
            num_subexp,
            onepass,
            prefix,
            prefix_bytes,
            prefix_complete,
            prefix_end,
            prefix_rune,
            prog,
            subexp_names,
        }
    }
}
